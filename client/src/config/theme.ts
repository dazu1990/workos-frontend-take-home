import type { ThemeProps } from "@radix-ui/themes";

export const themeConfig: ThemeProps = {
  appearance: "light",
  accentColor: "purple",
  grayColor: "gray",
  radius: "medium",
  scaling: "100%",
  panelBackground: "solid",
};
